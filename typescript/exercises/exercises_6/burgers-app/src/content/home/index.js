import {PageWrapper} from "../../common/page-wrapper";
import {Typography} from "@mui/material";

export const Home = () => {
    return <PageWrapper title="Home">
        <Typography>Welcome to oue restaurant!</Typography>
    </PageWrapper>
}